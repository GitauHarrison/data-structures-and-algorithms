// Return the minimum number in a list of numbers entered by a user.

// Create a method called readIntegers that reads a comma delimited list of numbers
// entered by a user from the console and then returns the array of those numbers

// Write another method called findMin that takes the array as an argument and returns 
// the minimum value found in that array

// In the main method, calle the method readIntegers to get the integers from the user 
// Pass the array to findMin method and print the minimum element in the array.